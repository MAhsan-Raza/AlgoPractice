#pragma once


IListNode* MergeKSortedLists_DNC(const vector<IListNode*>& lists)
{
	//merge 1st with second & the with third & so on
	IListNode* head = nullptr;
	return head;
}

void testDivideAndConquer()
{
}